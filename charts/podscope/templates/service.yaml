apiVersion: v1
kind: Service
metadata:
  name: {{ include "podscope.fullname" . }}
  labels:
    {{- include "podscope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podscope.selectorLabels" . | nindent 4 }}
